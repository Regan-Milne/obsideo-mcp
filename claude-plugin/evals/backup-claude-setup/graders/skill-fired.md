---
type: tool_used
tool: Skill
input_match: backup-claude-setup
---
Should fire on a directive to preserve the Claude Code setup.

Note: a question-shaped prompt ("what should I do?") gets the skill NAMED and
offered rather than invoked. That is correct behaviour for a skill that writes
files and uploads, so this case is deliberately imperative.
